TODO for gerbv (priorities mixed)
$Id$

* a batch backend generating tiff files (open).
* improve ps backend (open).
* hpgl backend (open).
* improve drill file parser (pitch).
* zooming centered around mouse (open, spe?)
* handle gerber layers (spe)
